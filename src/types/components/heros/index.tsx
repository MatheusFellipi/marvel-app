export type TypeCharacters = {
  id: number;
  name?: string;
  title?: string;
  thumbnail: {
    path: string;
    extension: string;
  };
};

export type TypeCharactersDetails = {
  id: number;
  name: string;

};
