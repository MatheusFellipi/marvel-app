import { Text, View } from "react-native";

const LoginScreen = () => {
  return (
    <View
      style={{
        justifyContent: "center",
      }}
    >
      <Text>Test</Text>
    </View>
  );
};

export default LoginScreen;
